{
  "version": 2,
  "name": "smartflow-ai",
  "builds": [
    {
      "src": "server/serverless.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/serverless.js"
    },
    {
      "src": "/uploads/(.*)",
      "dest": "/server/serverless.js"
    },
    {
      "src": "/(.*)",
      "dest": "/client/$1"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 